dist: xenial
language: minimal
sudo: required

addons:
  apt:
    update: true
    packages:
      - latex-beamer
      - texlive-luatex
      - latexmk

script:
- latexmk -lualatex -interaction=nonstopmode example.tex
