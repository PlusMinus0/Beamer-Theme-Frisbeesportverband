dist: xenial
language: minimal
sudo: required

addons:
  apt:
    update: true
    packages:
      - texlive-latex-recommended
      - texlive-luatex
      - latexmk

script:
- latexmk -f -lualatex -interaction=nonstopmode example.tex
